name: untitled1
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0' # Cập nhật phạm vi SDK

# pubspec.yaml

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  path_provider: ^2.1.0
  provider: ^6.1.0
  intl: ^0.19.0

  # THÊM GÓI NÀY để hỗ trợ chạy trên Windows/Desktop
  sqflite_common_ffi: ^2.3.1

  cupertino_icons: ^1.0.8

# ... (phần còn lại không đổi)

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0 # Cập nhật lên phiên bản lint mới hơn (3.0.0 thay vì 6.0.0)

flutter:


  uses-material-design: true